[project]
name = "serial-validator"
version = "0.1.0"
description = "REST API for serial key authentication with per-machine fingerprint binding"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "alembic>=1.18.4",
    "fastapi>=0.135.1",
    "python-dotenv>=1.2.2",
    "sqlalchemy>=2.0.48",
    "uvicorn>=0.42.0",
]
